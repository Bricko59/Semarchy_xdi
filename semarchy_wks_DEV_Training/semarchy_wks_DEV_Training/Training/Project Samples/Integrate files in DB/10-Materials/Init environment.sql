DROP SCHEMA LOAD_FILES CASCADE ;
CREATE SCHEMA LOAD_FILES;

CREATE TABLE LOAD_FILES.DATA_FILE1
(
id_record   INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
file_id     INTEGER,
last_name   VARCHAR(100),
first_name  VARCHAR(100),
file_name   VARCHAR(100),
file_date   CHAR(8),
update_date TIMESTAMP);
CREATE TABLE LOAD_FILES.DATA_FILE2
(
id_record   INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
file_id     INTEGER,
last_name   VARCHAR(100),
first_name  VARCHAR(100),
file_name   VARCHAR(100),
file_date   CHAR(8),
update_date TIMESTAMP);
CREATE TABLE LOAD_FILES.DATA_FILE3
(
id_record   INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
file_id     INTEGER,
last_name   VARCHAR(100),
first_name  VARCHAR(100),
file_name   VARCHAR(100),
file_date   CHAR(8),
update_date TIMESTAMP);